# Story 22.3: TODO/FIXME Tracker

## Objective

List all TODO, FIXME, HACK, XXX comments in source code with filtering and sorting.

## Agent

@dev (Dex)

## Status

InReview

## Story

**As a** developer,
**I want** a TODO/FIXME tracker,
**so that** I can keep track of technical debt and pending tasks in the codebase.

## Acceptance Criteria

- [x] `aiox todos` lists all TODO, FIXME, HACK, XXX comments in source
- [x] `aiox todos --type TODO` filters by type
- [x] `aiox todos --format json` outputs as JSON
- [x] `aiox todos --assignee <name>` filters by assignee (TODO(@name))
- [x] `aiox todos --count` just shows counts
- [x] `aiox todos --sort priority` sorts by type priority (FIXME > TODO > HACK > XXX)
- [x] Command registered in bin/aiox.js
- [x] 15+ tests passing

## File List

- `.aiox-core/cli/commands/todos/index.js` — Command implementation
- `tests/cli/todos.test.js` — Test suite (15+ tests)
- `bin/aiox.js` — Command registration
- `docs/stories/22.3.story.md` — This story
