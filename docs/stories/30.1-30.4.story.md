# Sprint 30: Network & HTTP Tools

**Status:** InReview
**Sprint:** 30
**Stories:** 30.1, 30.2, 30.3, 30.4

---

## Story 30.1: Ping & Latency — `aiox ping`

**Status:** InReview

### Acceptance Criteria
- [x] `aiox ping <host>` — TCP ping to host, show latency
- [x] `aiox ping <host> --count N` — ping N times
- [x] `aiox ping <host> --port 443` — specific port
- [x] `aiox ping <host> --format json` — as JSON
- [x] Uses `net.Socket` connect timing
- [x] 15+ tests passing

### File List
- `.aiox-core/cli/commands/ping/index.js`
- `tests/cli/ping.test.js`
- `bin/aiox.js` (case added)

---

## Story 30.2: HTTP Health Checker — `aiox http-check`

**Status:** InReview

### Acceptance Criteria
- [x] `aiox http-check <url>` — check URL status code, response time, headers
- [x] `aiox http-check <url> --expect 200` — exit 1 if status doesn't match
- [x] `aiox http-check <url> --format json` — as JSON
- [x] `aiox http-check <url> --headers` — show response headers
- [x] Uses Node.js http/https modules
- [x] 15+ tests passing

### File List
- `.aiox-core/cli/commands/http-check/index.js`
- `tests/cli/http-check.test.js`
- `bin/aiox.js` (case added)

---

## Story 30.3: Port Scanner — `aiox port-scan`

**Status:** InReview

### Acceptance Criteria
- [x] `aiox port-scan <host>` — scan common ports (80, 443, 22, 3000, 5432, 8080)
- [x] `aiox port-scan <host> --range 1-1024` — custom range
- [x] `aiox port-scan <host> --format json` — as JSON
- [x] `aiox port-scan <host> --timeout 1000` — connection timeout ms
- [x] Uses `net.Socket` with timeout
- [x] 15+ tests passing

### File List
- `.aiox-core/cli/commands/port-scan/index.js`
- `tests/cli/port-scan.test.js`
- `bin/aiox.js` (case added)

---

## Story 30.4: DNS Lookup — `aiox dns-lookup`

**Status:** InReview

### Acceptance Criteria
- [x] `aiox dns-lookup <domain>` — shows A, AAAA, MX, NS, TXT records
- [x] `aiox dns-lookup <domain> --type A` — specific record type
- [x] `aiox dns-lookup <domain> --format json` — as JSON
- [x] `aiox dns-lookup <domain> --all` — all record types
- [x] Uses `dns.promises` module
- [x] 15+ tests passing

### File List
- `.aiox-core/cli/commands/dns-lookup/index.js`
- `tests/cli/dns-lookup.test.js`
- `bin/aiox.js` (case added)
