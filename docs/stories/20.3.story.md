# Story 20.3: Release Checklist Runner

## Objective

Run pre-release checks and report pass/fail status for all release criteria.

## Agent

@dev (Dex)

## Status

InReview

## Story

**As a** developer,
**I want** an automated release checklist,
**so that** I can verify all criteria before releasing.

## Acceptance Criteria

- [x] `aiox release-check` runs pre-release checklist and reports pass/fail
- [x] Checks: tests pass, lint clean, no uncommitted changes, version bumped, changelog updated, no TODOs in source, dependencies up to date
- [x] `aiox release-check --fix` auto-fix flag available
- [x] `aiox release-check --format json` as JSON
- [x] `aiox release-check --ci` exits 1 if any check fails
- [x] Command registered in bin/aiox.js
- [x] 15+ tests passing

## File List

- `.aiox-core/cli/commands/release-check/index.js` (command)
- `tests/cli/release-check.test.js` (tests — 19 tests)
- `bin/aiox.js` (registration)

## Change Log

- 2026-04-08: Story created and implemented by @dev
