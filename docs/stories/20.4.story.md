# Story 20.4: CLI Stats & Summary

## Objective

Provide a comprehensive project summary with counts for commands, tests, stories, LOC, sprints, PRs, and dependencies.

## Agent

@dev (Dex)

## Status

InReview

## Story

**As a** developer,
**I want** a project stats summary,
**so that** I can see the overall health and scope of the project at a glance.

## Acceptance Criteria

- [x] `aiox stats-summary` shows comprehensive project summary
- [x] Shows: total commands, total test files, total stories, total lines of code, sprints completed, PRs merged, dependencies count
- [x] `aiox stats-summary --format json` as JSON
- [x] `aiox stats-summary --badge` generates badge URLs for README
- [x] `aiox stats-summary --output path` saves to file
- [x] Command registered in bin/aiox.js
- [x] 15+ tests passing

## File List

- `.aiox-core/cli/commands/stats-summary/index.js` (command)
- `tests/cli/stats-summary.test.js` (tests — 34 tests)
- `bin/aiox.js` (registration)

## Change Log

- 2026-04-08: Story created and implemented by @dev
