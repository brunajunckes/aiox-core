# Story 31.3 — Development Log

**Status:** InReview
**Sprint:** 31 — Developer Productivity
**Module:** `.aiox-core/cli/commands/devlog/`

## Description

Implement `aiox devlog` command for timestamped development log entries with tagging, filtering, search, export, and clear. Store in `.aiox/devlog.jsonl`.

## Acceptance Criteria

- [x] `aiox devlog add "<message>"` adds log entry with timestamp
- [x] `aiox devlog add "<message>" --tag feature` with tag
- [x] `aiox devlog list` shows recent entries
- [x] `aiox devlog list --tag bug` filter by tag
- [x] `aiox devlog search <term>` search entries
- [x] `aiox devlog export` export as JSON
- [x] `aiox devlog clear` clear log
- [x] Store: `.aiox/devlog.jsonl`
- [x] Registered in `bin/aiox.js`
- [x] 15+ tests passing

## File List

| File | Action |
|------|--------|
| `.aiox-core/cli/commands/devlog/index.js` | Created |
| `tests/cli/devlog.test.js` | Created |
| `bin/aiox.js` | Modified (added case) |
| `docs/stories/31.3.story.md` | Created |

## Test Results

- 30 tests passing
- Covers: loadEntries, addEntry, listEntries, searchEntries, exportEntries, clearEntries, generateId, runDevlog, exports
